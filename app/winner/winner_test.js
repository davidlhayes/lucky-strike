  'use strict';

  describe('myApp.winner module', function() {

    beforeEach(module('myApp.winner'));

    describe('winner controller', function(){

      it('should ....', inject(function($controller) {
        //spec body
        var WinnerCtrl = $controller('WinnerCtrl');
        expect(WinnerCtrl).toBeDefined();
      }));

    });
  });
